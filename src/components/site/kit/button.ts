/**
 * Shared CTA class strings — one calm pill button and one quiet text link,
 * reused across hero, contact, and CTAs so every action looks identical.
 * Monochrome only (ink/background), no glow, no transform jump.
 */
export const btnPrimary =
  "inline-flex items-center justify-center rounded-full bg-foreground px-5 py-2.5 text-sm font-medium text-background transition-opacity hover:opacity-90";

export const btnGhost =
  "inline-flex items-center gap-1.5 text-sm text-muted-foreground transition-colors hover:text-foreground";
